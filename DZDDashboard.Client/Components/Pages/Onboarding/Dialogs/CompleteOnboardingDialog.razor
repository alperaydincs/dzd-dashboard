@using DZDDashboard.Common.Constants
@using DZDDashboard.Common.DTOs
@inject DZDDashboard.Client.Localization.AppLocalizer Loc

<MudDialog>
    <TitleContent>
        <div class="d-flex align-center gap-2">
            <MudIcon Icon="@DzdIcons.CircleCheck" Color="Color.Success" Size="Size.Small" />
            <MudText Typo="Typo.h6">@Loc["onboardingDetail.completeOnboarding"]</MudText>
        </div>
    </TitleContent>
    <DialogContent>
        <MudText Typo="Typo.body2" Class="mb-3">
            @((MarkupString)string.Format(Loc["completeOnboardingDialog.message"], $"<b>{Process.EmployeeName}</b>"))
        </MudText>
        <MudList T="string" Dense="true">
            @foreach (var item in Process.Items)
            {
                <MudListItem T="string" Icon="@(item.Status == ChecklistItemStatuses.Completed ? DzdIcons.CircleCheck : DzdIcons.Circle)"
                             IconColor="@(item.Status == ChecklistItemStatuses.Completed ? Color.Success : Color.Default)">
                    @item.Title
                </MudListItem>
            }
        </MudList>
    </DialogContent>
    <DialogActions>
        <MudButton Variant="Variant.Outlined" OnClick="Cancel">@Loc["completeOnboardingDialog.no"]</MudButton>
        <MudButton Variant="Variant.Filled" Color="Color.Success" OnClick="Submit">@Loc["completeOnboardingDialog.yesComplete"]</MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] private IMudDialogInstance MudDialog { get; set; } = default!;
    [Parameter] public OnboardingProcessDto Process { get; set; } = default!;

    private void Submit() => MudDialog.Close(DialogResult.Ok(true));
    private void Cancel() => MudDialog.Cancel();
}
